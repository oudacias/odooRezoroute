<?xml version="1.0" encoding="utf-8"?>
    <odoo>
        <record model="ir.ui.view" id="ps_rezoroute.UserPos">
            <field name="name">ps_rezoroute POS USER</field>
            <field name="model">pos.config</field>
            <field name="inherit_id" ref="point_of_sale.pos_config_view_form"/>
            <field name="type">form</field>

            <field name="arch" type="xml">   
               
                <xpath expr="//div[hasclass('oe_title')]" position="inside">
                    <group>
                        <!-- <label for="user_id"/> -->
                        <field name="user_id"/>

                        <!-- <label for="location_id"/> -->
                        <field name="location_id"/>
                    </group>
                        
                </xpath>
            </field>
        </record>
        <record model="ir.ui.view" id="ps_rezoroute.UserPos3">
            <field name="name">ps_rezoroute POS USER</field>
            <field name="model">pos.config</field>
            <field name="inherit_id" ref="point_of_sale.view_pos_config_kanban"/>
            <field name="type">kanban</field>

            <field name="arch" type="xml">   
                <xpath expr="//button[@name='open_existing_session_cb']" position="replace">               
                    <button t-if="record.current_session_state.raw_value === 'opening_control'" class="btn btn-primary" name="open_existing_session_cb" type="object">Consulter la Session</button>
                    <br/>
                    <br/>
                    <button t-if="record.current_session_state.raw_value === 'opening_control'" class="btn btn-primary" name="check_cash_funds" type="object">Valider la Session</button>


                </xpath>
            </field>
        </record>
    </odoo>